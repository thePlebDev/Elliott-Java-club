const myParser = function(options){
    let token = [],
    types = [],
    parse = function(){ //This is going to be the actual parser/lexer
        let data = options.input
        .input
        .toString()

    }
}